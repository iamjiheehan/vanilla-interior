{
  "name": "vanilla-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:css && npm run build:js && npm run build:html",
    "build:css": "mkdir -p vanilla-project/src/css && cp src/css/index.css vanilla-project/src/css/style.css",
    "build:js": "browserify src/js/main.js -o vanilla-project/js/bundle.js",
    "build:html": "cp src/index.html vanilla-project/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "drag-drop-touch": "^1.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
